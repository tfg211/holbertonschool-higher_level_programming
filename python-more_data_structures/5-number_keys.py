#!/usr/bin/python3
def number_keys(a_dictionary):
    """
    Returns the number of keys in a dictionary.
    """
    # The len() function returns the number of items (keys) in a dictionary.
    return len(a_dictionary)
